services:
  go2rtc:
    image: alexxit/go2rtc:latest
    container_name: camwall-go2rtc
    network_mode: host
    restart: unless-stopped
    volumes:
      - ./go2rtc.yaml:/config/go2rtc.yaml:ro
    environment:
      - TZ=Australia/Sydney
      - TAPO_PASS=${TAPO_PASS}
